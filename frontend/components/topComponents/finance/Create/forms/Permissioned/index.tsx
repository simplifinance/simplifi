import React from "react";

export const Permissioned = (props: {handleBack: () => void}) => {

    const { handleBack } = props;

    return(
        <div>
            Permissioned
        </div>
    );
}