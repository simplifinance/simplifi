import React from "react";
import { Common } from "./Common";

export const Closed : React.FC = () => {

  return(
    <Common 
      heroTitle2="Total Closed Pools"
      operation='Closed'
    />
  );
}